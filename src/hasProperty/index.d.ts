/**
 * check if the given object has the named property.
 * @param object target object
 * @param property property name
 */
export default function hasProperty(object: any, property: string): boolean;
